# DevPilot 中文使用说明

## 你只需要记住两件事

1. 启动：双击仓库根目录的 `启动-DevPilot.cmd`
2. 关闭：双击仓库根目录的 `关闭-DevPilot.cmd`

启动后使用 **↑ / ↓** 选择模式，按 **Enter** 启动，按 **Esc** 取消：

```text
> Cloudflare Quick Tunnel
  OpenAI Secure MCP Tunnel
```

## 第一次使用

环境要求：

- Windows 10 / 11
- Git
- Node.js `>=22.19 <27`
- npm

如果你是新手，推荐直接把 [AI_INSTALL_PROMPT.md](AI_INSTALL_PROMPT.md) 里的提示词交给一个能够操作本机终端和文件的 AI 编程助手，让它帮你完成安装、构建、测试和第一次启动。

如果你手动安装：

```powershell
git clone https://github.com/1210350468/devpilot.git
cd devpilot
npm install
npm run build
```

之后双击 `启动-DevPilot.cmd` 即可。

首次启动时，如果 `node_modules` 不存在，启动器也会自动执行依赖安装；如果 `dist` 不存在，会自动构建。

## 方案 A：Cloudflare Quick Tunnel

适合第一次使用和快速验证链路。

启动器会自动：

- 启动 DevPilot / DevSpace MCP，默认端口 `7681`
- 启动本地 Supervisor，默认端口 `7680`
- 使用 `authMode=oauth`
- 下载或复用 `cloudflared`
- 创建 `https://*.trycloudflare.com/mcp`
- 使用 `toolMode=full`
- 关闭 DevSpace widgets
- 将完整公网 MCP URL 复制到剪贴板

把启动器输出的：

```text
https://xxxxxxxx.trycloudflare.com/mcp
```

添加到支持 Remote MCP 的客户端，并按提示完成 OAuth。

> Quick Tunnel URL 重新启动后可能变化。

## 方案 B：OpenAI Secure MCP Tunnel

适合已经有 OpenAI Tunnel ID 和 Runtime API Key 的用户。

本地配置文件：

```text
<DevPilot仓库>\.devpilot-config\openai-tunnel.json
```

示例：

```json
{
  "tunnelId": "tunnel_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx",
  "runtimeApiKey": "YOUR_RUNTIME_API_KEY",
  "controlPlaneProxy": "http://127.0.0.1:YOUR_PROXY_PORT"
}
```

`controlPlaneProxy` 可省略。实际 Runtime API Key 不要提交到 Git，也不要贴到公开聊天或日志里。

OpenAI 模式会：

- 使用 `authMode=secure-tunnel`
- 下载或复用官方 `tunnel-client`
- 保持本地 MCP 在 `127.0.0.1:7681/mcp`
- 检查 `127.0.0.1:7683/readyz`
- 支持 OpenAI control-plane 专用 HTTP 代理
- 检查常见的 Tunnel ID 冲突

一个 Tunnel ID 不应同时被两个本地 `tunnel-client` 消费，否则 MCP session 和工具集合可能被拆到不同进程。

## 常用地址

```text
Control Center : http://127.0.0.1:7681/devpilot/
Local MCP      : http://127.0.0.1:7681/mcp
Supervisor     : http://127.0.0.1:7680
Tunnel ready   : http://127.0.0.1:7683/readyz   （仅 OpenAI 模式）
Tunnel UI      : http://127.0.0.1:7683/ui       （仅 OpenAI 模式）
```

## allowed roots

第一次启动且没有旧配置时，DevPilot 默认只允许访问 **DevPilot 仓库自身**。

打开：

```text
http://127.0.0.1:7681/devpilot/
```

在设置里添加你真正希望 ChatGPT 能操作的代码目录。不要为了方便把整个系统盘加入 allowed roots。

如果你的电脑以前已经使用过 DevSpace，启动器会优先复用已有的 DevPilot allowed roots，其次复用旧 DevSpace 的 allowed roots。

## OAuth discovery 日志

在 `secure-tunnel` 模式中，DevPilot 不广告公网 OAuth metadata。官方 `tunnel-client` 仍可能探测：

```text
/.well-known/oauth-protected-resource/mcp
/.well-known/oauth-protected-resource
```

DevPilot 会对这两个路径返回空 404，让客户端明确识别为“没有广告 OAuth metadata”，避免把默认 HTML 404 当 JSON 解析产生：

```text
invalid character '<' looking for beginning of value
```

Cloudflare 模式仍然使用 DevSpace 的 OAuth flow。

## 本地私有文件

以下内容默认不进入 Git：

```text
.devpilot-config/
.devpilot-runtime/
DevPilot-connection.txt
当前公网MCP地址.txt
tools/cloudflared.exe
tools/tunnel-client.exe
*.log
```

## 请求观察器

Control Center 中的请求观察器按：

```text
ChatGPT 对话
    ↓
该对话的 MCP 请求
    ↓
单条请求详情
```

展示请求，并记录工具名、参数摘要、HTTP 状态、耗时、User-Agent 和 MCP session 等信息。DevPilot 优先使用 ChatGPT 提供的 `openai/session` 做对话隔离。

## 关于 ChatGPT 工具弹出

`widgets=off` 只能关闭 DevSpace 自己提供的 MCP Widget/App UI。

ChatGPT 网页或客户端自身渲染的原生工具调用提示不受 MCP 服务端控制，DevPilot 无法强制隐藏。DevPilot 的作用是减少自己的额外 UI，并把详细调用过程集中到本地请求观察器。

## 出问题先看哪里

```text
.devpilot-runtime\devpilot.out.log
.devpilot-runtime\devpilot.err.log
DevPilot-connection.txt
```

如果是 OpenAI Secure Tunnel，再检查：

```text
http://127.0.0.1:7683/readyz
```

完整技术说明见：[docs/devpilot.md](docs/devpilot.md)。
